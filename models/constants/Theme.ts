export default interface Theme {
  text: string;
  background: string;
  tint: string;
  tabIconDefault: string;
  tabIconSelected: string;
  textColor: string;
  secondaryTextColor: string;
  linkColor: string;
  textInput: string;
  ratingColor: string;
}
